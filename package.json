{
  "name": "package-prepare",
  "private": true,
  "version": "0.0.0",
  "description": "Simple package to test which lifecycle scripts are run when using npm and yarn to install the package via git.",
  "main": "build.js",
  "files": [
    "output.txt"
  ],
  "scripts": {
    "prepare": "node build.js prepare",
    "prepublish": "node build.js prepublish",
    "prepublishOnly": "node build.js prepublishOnly",
    "prepack": "node build.js prepack",
    "postpack": "node build.js postpack",
    "dependencies": "node build.js dependencies"
  },
  "author": "Cameron Tacklind <cameron@tacklind.com>",
  "license": "ISC"
}
